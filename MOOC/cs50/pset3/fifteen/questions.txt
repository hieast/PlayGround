questions.txt

Computer Science 50
Problem Set 3

0. 
3 × 3, 5 × 5, 6 × 6, 7 × 7, 8 × 8, 9 × 9.
Actually, dimensions with size between DIM_MIN × DIM_MIN and DIM_MAX × DIM_MAX are all allowed.
And DIM_MIN / DIM_MAX can be changed in fifteen.c.

1. 
A two-dimensional array

2. 
void greet(void);

3. 
void init(void);
void draw(void);
bool move(int tile);
bool won(void);
